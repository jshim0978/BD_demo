import React from 'react';
import './Header.css'; // Make sure to create this CSS file for basic styling

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">My App</h1>  // Customize the title as needed
        </header>
    );
}

export default Header;
